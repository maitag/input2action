<?xml version="1.0" encoding="utf-8"?>
<project>
	
	<meta title="Input2Action" package="lime.samples.input2actions" version="0.7.0" company="semmi" />
	
	<app main="SimpleConfig" path="bin" file="Input2Action" />
	<!-- SimpleConfig -->
	<!-- KeyCombos -->
	<!-- MultiPlayer -->
	<!-- JsonInOut -->
	
	<source path="src" />
	
	<haxelib name="lime" />
	<haxelib name="json2object" />
	<haxelib name="input2action" />
	
	<!-- to output action names while debugging InputState -->
	<haxedef name="input2action_debug" />
	
	<!-- optimize if no need of key-combinations: -->
	<!-- <haxedef name="input2action_noKeyCombos" /> -->
	
	<!-- optimize if no need of repeatKeyboardDefault or custom repeatDelay and repeatRate settings -->
	<!-- <haxedef name="input2action_noRepeat" /> -->
	
	<haxeflag name="-dce" value="full" />
	
</project>